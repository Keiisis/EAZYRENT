<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET n'était déclaré QUE dans les manifestes debug et profile,
         où l'outillage Flutter l'injecte automatiquement. Le manifeste
         principal n'avait aucune permission : l'application aurait fonctionné
         parfaitement pendant tout le développement, puis se serait retrouvée
         totalement muette une fois publiée — ni Supabase, ni images, ni
         tuiles de carte. La panne ne serait apparue qu'en production. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Lecture du réseau : distingue « pas de connexion » de « le serveur
         ne répond pas ». Les deux appellent des messages différents, et P8
         impose de dire lequel des deux se produit. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Position : demandée UNIQUEMENT sur geste explicite (« Autour de moi »,
         « Me localiser »), jamais au démarrage. COARSE suffit à trier des
         biens à l'échelle du quartier ; FINE est déclarée parce que geolocator
         l'exige pour les appareils qui n'exposent pas le mode approximatif. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Notes vocales de la messagerie (F4). Rien n'enregistre sans que
         l'utilisateur maintienne le bouton. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Notifications Android 13+. L'amorce (A06) précède TOUJOURS ce
         dialogue : un refus système est définitif. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="eazyrent"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
